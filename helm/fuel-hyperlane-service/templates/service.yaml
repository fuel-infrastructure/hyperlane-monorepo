apiVersion: v1
kind: Service
metadata:
  name: {{ include "fuel-hyperlane-service.fullname" . }}
  labels:
    {{- include "fuel-hyperlane-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fuel-hyperlane-service.selectorLabels" . | nindent 4 }}
